-if session[:viewing_shared] == true
    = render 'shared', :memcard => memcard
- elsif memcard.question_type == 'text'
    = render 'short', :memcard => memcard
- else 
    = render 'multiple', :memcard => memcard